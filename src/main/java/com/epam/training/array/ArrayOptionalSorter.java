package com.epam.training.array;

//1.	Do array sorting with several algorithms (hint: one interface and several implementations);
public interface ArrayOptionalSorter {
    public Array sort(Array array);
}

